using System;
using neon;

namespace neongine
{
    /// <summary>
    /// Game draw system. When in Editor, it will only be enabled in Play mode.
    /// </summary>
    public interface IGameDrawSystem : IDrawSystem { }
}
