using neon;

namespace neongine
{
    /// <summary>
    /// Game update system. When in Editor, it will only be enabled in Play mode.
    /// </summary>
    public interface IGameUpdateSystem : IUpdateSystem { }
}
